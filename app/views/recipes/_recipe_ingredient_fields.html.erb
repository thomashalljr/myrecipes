<div class="ingredient">
  <%= f.collection_select :ingredient_id, Ingredient.all.order(:name), :id, :name %>
  <%= f.hidden_field :_destroy %>
  <%= link_to "remove", "javascript:void(0);", class: "remove-ingredient" %>
</div>
